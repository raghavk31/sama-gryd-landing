# BPP Caller routing rules
# Routes outgoing responses from BPP to BAP
routingRules:
  - version: "2.0.0"
    targetType: "bap"
    endpoints:
      - on_init
      - on_confirm
      - on_status
      - on_update
      - on_cancel

