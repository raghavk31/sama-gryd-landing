# BPP Receiver routing rules
# Routes incoming requests from BAP to BPP webhook
routingRules:
  - version: "2.0.0"
    targetType: "url"
    target:
      url: "http://sandbox-bpp:3002/api/webhook"
    endpoints:
      - init
      - confirm
      - status
      - update
      - cancel

