# BAP Caller routing rules
# Routes outgoing requests from BAP to BPP
routingRules:
  - version: "2.0.0"
    targetType: "bpp"
    endpoints:
      - init
      - confirm
      - status
      - update
      - cancel

